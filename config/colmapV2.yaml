systems:
  basedir: ".logs/"
  vis_every: 1000

model:
  # model_name: "HexPlane"
  N_voxel_init: 262144
  time_grid_init: 25 # according to github, this should be set to 0.25 * frame_number
  time_grid_final: 100 # according to github, this should be set to frame_number
  upsampling_type: aligned
  upsample_list: [3000, 5000, 7000]
  update_emptymask_list: [2000, 4000, 6000, 8000]
  # upsample_list: [300, 500, 700]
  # update_emptymask_list: [200, 400, 600, 800]
  # fusion_one: "multiply"
  # fusion_two: "multiply"
  # init_scale: 0.9
  # init_shift: 0.5
  # density_dim: 8
  # DensityMode: "general_MLP"

  # depth_loss: True
  # depth_loss_weight: 0.1

  density_n_comp: [24, 12, 12]
  app_n_comp: [48, 24, 24]
  fea2denseAct: relu

  # TV_t_s_ratio: 300

  TV_weight_density: 0.005
  TV_weight_app: 0.0005

  # density_n_comp: [24, 12, 12]
  # app_n_comp: [48, 24, 24]
  # fea2denseAct: relu
  # app_view_pe: 0
  # app_fea_pe: 0

  # TV_weight_density: 0.0005
  # TV_weight_app: 0.0005


data:
  datadir: ""
  # datadir: "/media/nico/TOSHIBA EXT/TUMRGBD/rgbd_dataset_freiburg3_sitting_rpy"
  dataset_name: own_data
  scene_bbox_min: [-5, -5, -5]
  scene_bbox_max: [5, 5, 5]
  N_vis: 1

  datasampler_type: rays
  # stage_1_iteration: 300000
  # stage_2_iteration: 250000
  # stage_3_iteration: 100000
  key_f_num: 30
  downsample: 1.0

  cal_fine_bbox: True

optim:
  n_iters: 20000

render_test: False
render_path: True
expname: tum_rgbd
